# Vite environment variables (loaded at build time)
# Copy to .env (for local dev) or pass as build-arg in Docker builds.

# Backend API base URL (include /api if your backend prefixes it)
VITE_API_URL=http://localhost:70/api

# Optional tweakables
VITE_API_TIMEOUT_MS=15000
VITE_API_WITH_CREDENTIALS=false
